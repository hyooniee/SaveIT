spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/saveit?useSSL=false&serverTimezone=Asia/Seoul
    username: root
    password: 1024

mybatis:
  mapper-locations: classpath:/mapper/*.xml
  type-aliases-package: com.saveit.vo
  configuration:
    map-underscore-to-camel-case: true

server:
  port: 8080
